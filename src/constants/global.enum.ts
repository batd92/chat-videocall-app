export enum TypeMessage {
    TEXT = "Text",
    FILE = "File",
    CALL = "Call",
}

export enum StatusJitsiMeet {
    INCOMING = "Incoming",
    RUNNING = "Running",
    ENDED = "Ended",
}

export enum TypeStorageMessage {
    IMAGE = "Image",
    VIDEO = "Video",
    PDF = "Pdf",
    Doc = "Doc",
    Link = "Link"
}

export enum TypeNotification {
    CHAT_MESSAGE = "ChatMessage",
}

export enum StatusAppUser {
    ONLINE = "Online",
    OFFLINE = "Offline"
}

export enum OS {
    ANDROID = "Android",
    IOS = "IOS",
    WEB = "Web"
}

export enum ETypeChanel {
    ROOM = "room",
    USER = "user"
}